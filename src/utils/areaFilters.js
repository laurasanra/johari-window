export const blindAreaFilter = (results) => {
  return results;
};

export const hiddenAreaFilter = (results) => {
  return results;
};

export const openAreaFilter = (results) => {
  return results;
};
